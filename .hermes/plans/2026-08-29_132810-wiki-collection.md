# Wiki-Collection — Plan de Implementación

> **Para Hermes:** Usa el skill `subagent-driven-development` para implementar este plan tarea por tarea.

**Goal:** Construir una wiki-colección web para gestionar libros, videojuegos, juegos de mesa, cartas Magic y películas/series. Fase 1: colección de libros con búsqueda vía API externa + CRUD backend + frontend.

**Architecture:** Aplicación web full-stack con backend Node.js/Express, MongoDB como base de datos, y frontend que consume tanto APIs externas (búsqueda) como la API propia (colección personal). La wiki del proyecto vive en el propio repo bajo `docs/`.

**Tech Stack:** Node.js, Express, MongoDB (Mongoose), React (frontend), Google Books API / Open Library API (búsqueda externa).

---

## Estructura del Repositoro

```
wiki-collection/
├── docs/                          # Wiki del proyecto
│   ├── README.md                  # Índice de la wiki
│   ├── architecture.md            # Decisiones de arquitectura
│   ├── data-models.md             # Modelos de datos por entidad
│   ├── api-reference.md           # Documentación de endpoints
│   ├── external-apis.md           # APIs externas usadas
│   ├── phase-1-books.md           # Plan detallado Fase 1
│   └── phase-2-games.md           # Plan detallado Fase 2 (futuro)
├── backend/
│   ├── src/
│   │   ├── models/
│   │   │   └── Book.js
│   │   ├── routes/
│   │   │   └── books.js
│   │   ├── controllers/
│   │   │   └── booksController.js
│   │   ├── config/
│   │   │   └── db.js
│   │   └── app.js
│   ├── package.json
│   └── .env.example
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   └── App.jsx
│   ├── package.json
│   └── vite.config.js
└── README.md
```

---

## Fase 1: Colección de Libros

### Task 1: Configurar estructura del proyecto y documentación base

**Objective:** Crear la estructura de carpetas, archivos base y la wiki inicial del proyecto.

**Files:**
- Crear: `docs/README.md`
- Crear: `docs/architecture.md`
- Crear: `docs/data-models.md`
- Crear: `docs/external-apis.md`
- Crear: `docs/api-reference.md`
- Crear: `docs/phase-1-books.md`
- Modificar: `README.md`

**Step 1: Crear estructura de directorios**

```bash
mkdir -p backend/src/{models,routes,controllers,config}
mkdir -p frontend/src/{components,pages,services}
mkdir -p docs
```

**Step 2: Escribir `docs/README.md` — Índice de la wiki**

```markdown
# Wiki — Proyecto Colección

Documentación del proyecto de gestión de colecciones personales.

## Contenido

- [Arquitectura](./architecture.md) — Decisiones técnicas y stack
- [Modelos de Datos](./data-models.md) — Esquemas por entidad
- [APIs Externas](./external-apis.md) — Servicios de terceros
- [API Reference](./api-reference.md) — Endpoints del backend
- [Fase 1: Libros](./phase-1-books.md) — Plan de implementación libros
```

**Step 3: Escribir `docs/architecture.md`**

```markdown
# Arquitectura

## Stack Tecnológico

| Capa      | Tecnología        | Motivo                          |
|-----------|-------------------|---------------------------------|
| Backend   | Node.js + Express | Ligero, rápido de prototipar    |
| Base de datos | MongoDB + Mongoose | Esquemas flexibles, JSON nativo |
| Frontend  + Vite | React            | Componentes, ecosistema amplio  |
| APIs externas | Google Books / Open Library | Gratuitas, sin auth para búsqueda |

## Patrón de Arquitectura

- **Backend:** MVC con separación de controllers, models y routes
- **Frontend:** Componentes funcionales con hooks, servicios para llamadas API
- **Comunicación:** REST JSON entre frontend y backend

## Decisiones

1. **MongoDB sobre SQL:** Los items de colección tienen atributos variables (un libro tiene autor, un juego tiene plataforma). MongoDB permite esquemas flexibles por entidad.
2. **Búsqueda externa + BD local:** La búsqueda de nuevos libros usa Google Books/Open Library. Los libros añadidos a la colección se guardan en BD local con datos enriquecidos por el usuario.
3. **Wiki en repo:** Toda la documentación vive en `docs/` para versionarla junto al código.
```

**Step 4: Escribir `docs/data-models.md`**

```markdown
# Modelos de Datos

## Book (Libro)

```javascript
{
  _id: ObjectId,           // MongoDB
  title: String,           // Título del libro
  authors: [String],       // Lista de autores
  isbn: String,            // ISBN-13 o ISBN-10 (único)
  publisher: String,       // Editorial
  publishedDate: Date,     // Fecha de publicación
  description: String,     // Sinopsis
  pageCount: Number,       // Número de páginas
  categories: [String],    // Géneros/categorías
  coverImage: String,      // URL a la portada
  language: String,        // Idioma (es, en, etc.)
  
  // Campos de colección personal
  status: String,          // 'reading' | 'completed' | 'wishlist' | 'abandoned'
  userRating: Number,      // 1-5 (valoración personal)
  notes: String,           // Notas personales
  dateAdded: Date,         // Cuándo se añadió a la colección
  dateCompleted: Date,     // Cuándo se terminó (si aplica)
  tags: [String],          // Tags personalizados
  
  // Metadata de la API externa
  externalSource: String,  // 'google_books' | 'open_library'
  externalId: String,      // ID en la API externa
  
  createdAt: Date,
  updatedAt: Date
}
```

## Game (Videojuego) — Fase 2

```javascript
{
  _id: ObjectId,
  title: String,
  platform: String,        // PS5, PC, Switch, etc.
  publisher: String,
  releaseDate: Date,
  genre: [String],
  coverImage: String,
  status: String,          // 'playing' | 'completed' | 'backlog' | 'wishlist'
  userRating: Number,
  hoursPlayed: Number,
  notes: String,
  dateAdded: Date,
  createdAt: Date,
  updatedAt: Date
}
```

## BoardGame (Juego de Mesa) — Fase 3

```javascript
{
  _id: ObjectId,
  title: String,
  publisher: String,
  minPlayers: Number,
  maxPlayers: Number,
  playTime: Number,        // Minutos
  categories: [String],
  coverImage: String,
  userRating: Number,
  dateAcquired: Date,
  notes: String,
  createdAt: Date,
  updatedAt: Date
}
```

## MagicCard (Carta Magic) — Fase 4

```javascript
{
  _id: ObjectId,
  name: String,
  manaCost: String,
  type: String,
  rarity: String,
  set: String,
  text: String,
  power: String,
  toughness: String,
  imageUrl: String,
  quantity: Number,
  condition: String,       // 'mint' | 'near_mint' | 'excellent' | ...
  language: String,
  foil: Boolean,
  notes: String,
  createdAt: Date,
  updatedAt: Date
}
```

## MovieShow (Película/Serie) — Fase 5

```javascript
{
  _id: ObjectId,
  title: String,
  type: String,            // 'movie' | 'series'
  director: String,
  cast: [String],
  genre: [String],
  releaseYear: Number,
  runtime: Number,         // Minutos (película) o duración media (serie)
  seasons: Number,         // Solo series
  episodes: Number,        // Solo series
  posterUrl: String,
  status: String,          // 'watching' | 'completed' | 'wishlist' | 'dropped'
  userRating: Number,
  watchedDate: Date,
  notes: String,
  createdAt: Date,
  updatedAt: Date
}
```
```

**Step 5: Escribir `docs/external-apis.md`**

```markdown
# APIs Externas

## Google Books API

- **Base URL:** `https://www.googleapis.com/books/v1/volumes`
- **Auth:** API Key (opcional para búsquedas básicas, recomendado para producción)
- **Rate limit:** 100 requests/100 segundos (con API Key), 10/sin key
- **Gratis:** Sí, con API Key gratuita desde Google Cloud Console

### Endpoints útiles

| Uso | Endpoint |
|-----|----------|
| Buscar por título | `GET /volumes?q=intitle:{title}` |
| Buscar por autor | `GET /volumes?q=inauthor:{author}` |
| Buscar por ISBN | `GET /volumes?q=isbn:{isbn}` |
| Obtener por ID | `GET /volumes/{volumeId}` |

### Ejemplo de respuesta (item)

```json
{
  "volumeInfo": {
    "title": "Cien años de soledad",
    "authors": ["Gabriel García Márquez"],
    "publisher": "Editorial Sudamericana",
    "publishedDate": "1967",
    "description": "La novela que...",
    "industryIdentifiers": [
      { "type": "ISBN_13", "identifier": "9780307474728" }
    ],
    "pageCount": 417,
    "categories": ["Fiction"],
    "imageLinks": {
      "thumbnail": "http://books.google.com/books/content?id=..."
    },
    "language": "es"
  }
}
```

## Open Library API

- **Base URL:** `https://openlibrary.org`
- **Auth:** No requerida
- **Rate limit:** ~100 requests/minuto (sin auth)
- **Gratis:** Sí, completamente

### Endpoints útiles

| Uso | Endpoint |
|-----|----------|
| Buscar | `GET /search.json?q={query}` |
| Buscar por ISBN | `GET /isbn/{isbn}.json` |
| Obtener obra | `GET /works/{workId}.json` |

### Ejemplo de respuesta (search)

```json
{
  "numFound": 123,
  "docs": [
    {
      "title": "Cien años de soledad",
      "author_name": ["Gabriel García Márquez"],
      "isbn": ["9780307474728"],
      "first_publish_year": 1967,
      "cover_i": 123456,
      "publisher": ["Editorial Sudamericana"]
    }
  ]
}
```

## Comparativa

| Característica | Google Books | Open Library |
|----------------|--------------|--------------|
| Portadas | ✅ Alta calidad | ✅ Calidad media |
| Sinopsis | ✅ Detalladas | ⚠️ Básicas |
| ISBN lookup | ✅ | ✅ |
| Rate limit generoso | ✅ (con key) | ⚠️ |
| Datos en español | ✅ | ⚠️ |

**Decisión:** Usar **Google Books API** como primaria (mejor calidad de datos, portadas, sinopsis). **Open Library** como fallback si Google Books no encuentra resultados.
```

**Step 6: Escribir `docs/api-reference.md`**

```markdown
# API Reference — Backend

## Base URL

```
http://localhost:3000/api
```

## Books Endpoints

### GET /api/books
Obtener todos los libros de la colección.

**Query params:**
- `status` — Filtrar por estado (`reading`, `completed`, `wishlist`, `abandoned`)
- `tag` — Filtrar por tag
- `sort` — Campo de ordenación (default: `-dateAdded`)
- `page` — Página (default: 1)
- `limit` — Items por página (default: 20)

**Response:**
```json
{
  "data": [ { ...book }, ... ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 42,
    "pages": 3
  }
}
```

### GET /api/books/:id
Obtener un libro por ID.

### POST /api/books
Añadir un libro a la colección.

**Body:**
```json
{
  "title": "string (required)",
  "authors": ["string"],
  "isbn": "string",
  "publisher": "string",
  "publishedDate": "date",
  "description": "string",
  "pageCount": 0,
  "categories": ["string"],
  "coverImage": "string (url)",
  "language": "string",
  "status": "wishlist | reading | completed | abandoned",
  "userRating": 1-5,
  "notes": "string",
  "tags": ["string"],
  "externalSource": "google_books | open_library",
  "externalId": "string"
}
```

### PUT /api/books/:id
Actualizar un libro existente.

### DELETE /api/books/:id
Eliminar un libro de la colección.

### GET /api/books/search?q={query}
Buscar libros en Google Books API (proxy del backend).

**Response:**
```json
{
  "source": "google_books",
  "results": [
    {
      "externalId": "string",
      "title": "string",
      "authors": ["string"],
      "isbn": "string",
      "coverImage": "string",
      "description": "string",
      "publishedDate": "string",
      "pageCount": 0,
      "publisher": "string"
    }
  ]
}
```

## Códigos de Estado

| Código | Significado |
|--------|-------------|
| 200 | OK |
| 201 | Created |
| 400 | Bad Request |
| 404 | Not Found |
| 500 | Server Error |
```

**Step 7: Escribir `docs/phase-1-books.md`**

```markdown
# Fase 1: Colección de Libros

## Objetivo
Permitir al usuario buscar libros vía API externa y gestionar su colección personal de libros.

## Pasos

### Paso 1: Búsqueda con API externa
- [ ] Configurar Google Books API Key
- [ ] Crear endpoint proxy `/api/books/search` en el backend
- [ ] Implementar fallback a Open Library si Google Books falla
- [ ] Crear componente de búsqueda en el frontend
- [ ] Mostrar resultados con portada, título, autor, año

### Paso 2: CRUD Backend
- [ ] Configurar conexión MongoDB
- [ ] Crear modelo Book con Mongoose
| Campo | Tipo | Requerido |
|-------|------|-----------|
| title | String | ✅ |
| authors | [String] | ✅ |
| isbn | String | ❌ |
| publisher | String | ❌ |
| publishedDate | Date | ❌ |
| description | String | ❌ |
| pageCount | Number | ❌ |
| categories | [String] | ❌ |
| coverImage | String | ❌ |
| language | String | ❌ |
| status | String | ✅ (default: 'wishlist') |
| userRating | Number | ❌ |
| notes | String | ❌ |
| tags | [String] | ❌ |
| externalSource | String | ❌ |
| externalId | String | ❌ |
| dateAdded | Date | auto |
| dateCompleted | Date | auto |
- [ ] Implementar controller con validaciones
- [ ] Crear rutas REST
- [ ] Añadir paginación y filtros

### Paso 3: Frontend
- [ ] Configurar proyecto React con Vite
- [ ] Crear servicio de API (booksService)
- [ ] Página de búsqueda
- [ ] Página de mi colección (lista/grid)
- [ ] Página de detalle de libro
- [ ] Formulario para añadir/editar libros
- [ ] Filtros por estado, tags, ordenación

## Criterios de Aceptación
- [ ] Buscar "Cien años de soledad" devuelve resultados correctos
- [ ] Añadir un libro a la colección desde búsqueda
- [ ] Ver lista de libros filtrada por estado
- [ ] Editar valoración y notas de un libro
- [ ] Eliminar un libro de la colección
- [ ] La wiki en `docs/` está actualizada
```

**Step 8: Actualizar `README.md` raíz**

```markdown
# Wiki-Collection

Aplicación web para gestionar colecciones personales.

## Fases

1. **Libros** — Búsqueda + CRUD + colección personal
2. Videojuegos *(futuro)*
3. Juegos de mesa *(futuro)*
4. Cartas Magic *(futuro)*
5. Películas y series *(futuro)*

## Documentación

Ver [docs/README.md](./docs/README.md) para la wiki completa del proyecto.

## Estructura

```
docs/       Wiki y documentación
backend/    API Node.js + MongoDB
frontend/   App React
```
```

**Step 9: Commit**

```bash
git add -A
git commit -m "docs: add project wiki, data models, architecture docs, and phase 1 plan"
```

---

### Task 2: Configurar backend Node.js + Express

**Objective:** Crear la base del backend con Express, conexión MongoDB, y estructura MVC.

**Files:**
- Crear: `backend/package.json`
- Crear: `backend/.env.example`
- Crear: `backend/src/config/db.js`
- Crear: `backend/src/app.js`
- Crear: `backend/src/server.js`

**Step 1: Crear `backend/package.json`**

```json
{
  "name": "wiki-collection-backend",
  "version": "1.0.0",
  "description": "Backend API for wiki-collection",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongoose": "^8.6.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}
```

**Step 2: Crear `backend/.env.example`**

```env
PORT=3000
MONGODB_URI=mongodb://localhost:27017/wiki-collection
GOOGLE_BOOKS_API_KEY=your_api_key_here
NODE_ENV=development
```

**Step 3: Crear `backend/src/config/db.js`**

```javascript
const mongoose = require('mongoose');

const connectDB = async () => {
  try {
    const conn = await mongoose.connect(process.env.MONGODB_URI);
    console.log(`MongoDB conectado: ${conn.connection.host}`);
  } catch (error) {
    console.error(`Error conectando a MongoDB: ${error.message}`);
    process.exit(1);
  }
};

module.exports = connectDB;
```

**Step 4: Crear `backend/src/app.js`**

```javascript
const express = require('express');
const cors = require('cors');
const booksRoutes = require('./routes/books');

const app = express();

app.use(cors());
app.use(express.json());

app.use('/api/books', booksRoutes);

app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

module.exports = app;
```

**Step 5: Crear `backend/src/server.js`**

```javascript
require('dotenv').config();
const app = require('./app');
const connectDB = require('./config/db');

const PORT = process.env.PORT || 3000;

const start = async () => {
  await connectDB();
  app.listen(PORT, () => {
    console.log(`Servidor corriendo en puerto ${PORT}`);
  });
};

start();
```

**Step 6: Instalar dependencias**

```bash
cd backend && npm install
```

**Step 7: Verificar que el servidor arranca**

```bash
cd backend && npm run dev
```

Expected: `Servidor corriendo en puerto 3000` (fallará sin MongoDB, pero la estructura es correcta)

**Step 8: Commit**

```bash
git add backend/
git commit -m "feat(backend): setup express server with mongodb connection"
```

---

### Task 3: Crear modelo Book con Mongoose

**Objective:** Definir el esquema y modelo de Book con validaciones e índices.

**Files:**
- Crear: `backend/src/models/Book.js`

**Step 1: Escribir el modelo**

```javascript
const mongoose = require('mongoose');

const bookSchema = new mongoose.Schema(
  {
    title: {
      type: String,
      required: [true, 'El título es obligatorio'],
      trim: true,
      index: true,
    },
    authors: {
      type: [String],
      required: [true, 'Al menos un autor es obligatorio'],
      default: [],
    },
    isbn: {
      type: String,
      trim: true,
      unique: true,
      sparse: true, // Permite múltiples null/undefined
    },
    publisher: {
      type: String,
      trim: true,
    },
    publishedDate: {
      type: Date,
    },
    description: {
      type: String,
      trim: true,
    },
    pageCount: {
      type: Number,
      min: [0, 'Las páginas no pueden ser negativas'],
    },
    categories: {
      type: [String],
      default: [],
    },
    coverImage: {
      type: String,
      trim: true,
    },
    language: {
      type: String,
      default: 'es',
    },
    status: {
      type: String,
      enum: ['wishlist', 'reading', 'completed', 'abandoned'],
      default: 'wishlist',
    },
    userRating: {
      type: Number,
      min: [1, 'Mínimo 1'],
      max: [5, 'Máximo 5'],
    },
    notes: {
      type: String,
      trim: true,
    },
    tags: {
      type: [String],
      default: [],
    },
    dateAdded: {
      type: Date,
      default: Date.now,
    },
    dateCompleted: {
      type: Date,
    },
    externalSource: {
      type: String,
      enum: ['google_books', 'open_library', null],
    },
    externalId: {
      type: String,
    },
  },
  {
    timestamps: true, // createdAt, updatedAt
  }
);

// Índices para búsquedas comunes
bookSchema.index({ title: 'text', authors: 'text' });
bookSchema.index({ status: 1 });
bookSchema.index({ tags: 1 });
bookSchema.index({ dateAdded: -1 });

// Actualizar dateCompleted automáticamente
bookSchema.pre('save', function (next) {
  if (this.isModified('status') && this.status === 'completed' && !this.dateCompleted) {
    this.dateCompleted = new Date();
  }
  next();
});

module.exports = mongoose.model('Book', bookSchema);
```

**Step 2: Commit**

```bash
git add backend/src/models/Book.js
git commit -m "feat(backend): add Book model with validations and indexes"
```

---

### Task 4: Crear controller CRUD para libros

**Objective:** Implementar toda la lógica de negocio para el CRUD de libros.

**Files:**
- Crear: `backend/src/controllers/booksController.js`

**Step 1: Escribir el controller**

```javascript
const Book = require('../models/Book');

// GET /api/books - Obtener todos los libros (con filtros y paginación)
exports.getBooks = async (req, res) => {
  try {
    const { status, tag, sort = '-dateAdded', page = 1, limit = 20 } = req.query;

    const filter = {};
    if (status) filter.status = status;
    if (tag) filter.tags = tag;

    const skip = (Number(page) - 1) * Number(limit);

    const [books, total] = await Promise.all([
      Book.find(filter).sort(sort).skip(skip).limit(Number(limit)),
      Book.countDocuments(filter),
    ]);

    res.json({
      data: books,
      pagination: {
        page: Number(page),
        limit: Number(limit),
        total,
        pages: Math.ceil(total / Number(limit)),
      },
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};

// GET /api/books/:id - Obtener un libro
exports.getBook = async (req, res) => {
  try {
    const book = await Book.findById(req.params.id);
    if (!book) return res.status(404).json({ error: 'Libro no encontrado' });
    res.json(book);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};

// POST /api/books - Crear libro
exports.createBook = async (req, res) => {
  try {
    const book = new Book(req.body);
    await book.save();
    res.status(201).json(book);
  } catch (error) {
    if (error.code === 11000) {
      return res.status(400).json({ error: 'Ya existe un libro con ese ISBN' });
    }
    if (error.name === 'ValidationError') {
      return res.status(400).json({ error: error.message });
    }
    res.status(500).json({ error: error.message });
  }
};

// PUT /api/books/:id - Actualizar libro
exports.updateBook = async (req, res) => {
  try {
    const book = await Book.findByIdAndUpdate(req.params.id, req.body, {
      new: true,
      runValidators: true,
    });
    if (!book) return res.status(404).json({ error: 'Libro no encontrado' });
    res.json(book);
  } catch (error) {
    if (error.name === 'ValidationError') {
      return res.status(400).json({ error: error.message });
    }
    res.status(500).json({ error: error.message });
  }
};

// DELETE /api/books/:id - Eliminar libro
exports.deleteBook = async (req, res) => {
  try {
    const book = await Book.findByIdAndDelete(req.params.id);
    if (!book) return res.status(404).json({ error: 'Libro no encontrado' });
    res.json({ message: 'Libro eliminado', id: req.params.id });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};

// GET /api/books/search?q=query - Buscar en Google Books API
exports.searchBooks = async (req, res) => {
  try {
    const { q } = req.query;
    if (!q) return res.status(400).json({ error: 'Query requerido' });

    const apiKey = process.env.GOOGLE_BOOKS_API_KEY;
    const url = `https://www.googleapis.com/books/v1/volumes?q=${encodeURIComponent(q)}&maxResults=20${
      apiKey ? `&key=${apiKey}` : ''
    }`;

    const response = await fetch(url);
    const data = await response.json();

    const results = (data.items || []).map((item) => ({
      externalId: item.id,
      title: item.volumeInfo?.title || 'Sin título',
      authors: item.volumeInfo?.authors || [],
      isbn: item.volumeInfo?.industryIdentifiers?.find((i) => i.type === 'ISBN_13')?.identifier || null,
      publisher: item.volumeInfo?.publisher || null,
      publishedDate: item.volumeInfo?.publishedDate || null,
      description: item.volumeInfo?.description || null,
      pageCount: item.volumeInfo?.pageCount || null,
      categories: item.volumeInfo?.categories || [],
      coverImage: item.volumeInfo?.imageLinks?.thumbnail || null,
      language: item.volumeInfo?.language || null,
    }));

    res.json({ source: 'google_books', results });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
```

**Step 2: Commit**

```bash
git add backend/src/controllers/booksController.js
git commit -m "feat(backend): add books CRUD controller with search proxy"
```

---

### Task 5: Crear rutas de libros

**Objective:** Definir las rutas REST y conectarlas al controller.

**Files:**
- Crear: `backend/src/routes/books.js`

**Step 1: Escribir las rutas**

```javascript
const express = require('express');
const router = express.Router();
const {
  getBooks,
  getBook,
  createBook,
  updateBook,
  deleteBook,
  searchBooks,
} = require('../controllers/booksController');

// Búsqueda en API externa (antes de :id para no colisionar)
router.get('/search', searchBooks);

// CRUD
router.get('/', getBooks);
router.get('/:id', getBook);
router.post('/', createBook);
router.put('/:id', updateBook);
router.delete('/:id', deleteBook);

module.exports = router;
```

**Step 2: Verificar que app.js ya importa las rutas**

(Debería estar ya del Task 2)

**Step 3: Commit**

```bash
git add backend/src/routes/books.js
git commit -m "feat(backend): add books routes"
```

---

### Task 6: Crear frontend React con Vite

**Objective:** Configurar el proyecto frontend con Vite y React.

**Files:**
- Crear: `frontend/package.json`
- Crear: `frontend/vite.config.js`
- Crear: `frontend/index.html`
- Crear: `frontend/src/main.jsx`
- Crear: `frontend/src/App.jsx`

**Step 1: Crear `frontend/package.json`**

```json
{
  "name": "wiki-collection-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
```

**Step 2: Crear `frontend/vite.config.js`**

```javascript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:3000',
    },
  },
});
```

**Step 3: Crear `frontend/index.html`**

```html
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wiki Collection</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

**Step 4: Crear `frontend/src/main.jsx`**

```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**Step 5: Crear `frontend/src/App.jsx`**

```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Mi Colección</Link>
        <Link to="/search">Buscar</Link>
      </nav>
      <Routes>
        <Route path="/" element={<h1>Mi Colección de Libros</h1>} />
        <Route path="/search" element={<h1>Buscar Libros</h1>} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

**Step 6: Instalar dependencias**

```bash
cd frontend && npm install
```

**Step 7: Verificar que arranca**

```bash
cd frontend && npm run dev
```

Expected: Vite dev server en `http://localhost:5173`

**Step 8: Commit**

```bash
git add frontend/
git commit -m "feat(frontend): setup react app with vite and router"
```

---

### Task 7: Crear servicio de API en el frontend

**Objective:** Crear un servicio centralizado para todas las llamadas API.

**Files:**
- Crear: `frontend/src/services/booksService.js`

**Step 1: Escribir el servicio**

```javascript
const API_URL = '/api/books';

export const booksService = {
  // Buscar libros en Google Books (vía backend proxy)
  search: async (query) => {
    const res = await fetch(`${API_URL}/search?q=${encodeURIComponent(query)}`);
    if (!res.ok) throw new Error('Error en la búsqueda');
    return res.json();
  },

  // Obtener libros de la colección
  getAll: async (params = {}) => {
    const queryString = new URLSearchParams(params).toString();
    const res = await fetch(`${API_URL}?${queryString}`);
    if (!res.ok) throw new Error('Error obteniendo libros');
    return res.json();
  },

  // Obtener un libro
  getById: async (id) => {
    const res = await fetch(`${API_URL}/${id}`);
    if (!res.ok) throw new Error('Libro no encontrado');
    return res.json();
  },

  // Añadir libro a la colección
  create: async (bookData) => {
    const res = await fetch(API_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(bookData),
    });
    if (!res.ok) throw new Error('Error creando libro');
    return res.json();
  },

  // Actualizar libro
  update: async (id, bookData) => {
    const res = await fetch(`${API_URL}/${id}`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(bookData),
    });
    if (!res.ok) throw new Error('Error actualizando libro');
    return res.json();
  },

  // Eliminar libro
  delete: async (id) => {
    const res = await fetch(`${API_URL}/${id}`, { method: 'DELETE' });
    if (!res.ok) throw new Error('Error eliminando libro');
    return res.json();
  },
};
```

**Step 2: Commit**

```bash
git add frontend/src/services/booksService.js
git commit -m "feat(frontend): add books API service"
```

---

### Task 8: Crear página de búsqueda

**Objective:** Implementar la interfaz de búsqueda de libros con Google Books.

**Files:**
- Crear: `frontend/src/pages/SearchPage.jsx`
- Crear: `frontend/src/components/BookCard.jsx`
- Modificar: `frontend/src/App.jsx`

**Step 1: Crear `frontend/src/components/BookCard.jsx`**

```jsx
function BookCard({ book, onAdd }) {
  return (
    <div className="book-card">
      <img src={book.coverImage || '/placeholder.png'} alt={book.title} />
      <h3>{book.title}</h3>
      <p>{book.authors?.join(', ')}</p>
      <p>{book.publishedDate}</p>
      {onAdd && <button onClick={() => onAdd(book)}>Añadir a colección</button>}
    </div>
  );
}

export default BookCard;
```

**Step 2: Crear `frontend/src/pages/SearchPage.jsx`**

```jsx
import { useState } from 'react';
import { booksService } from '../services/booksService';
import BookCard from '../components/BookCard';

function SearchPage() {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  const handleSearch = async (e) => {
    e.preventDefault();
    if (!query.trim()) return;

    setLoading(true);
    setError(null);
    try {
      const data = await booksService.search(query);
      setResults(data.results);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  const handleAdd = async (book) => {
    try {
      await booksService.create({
        title: book.title,
        authors: book.authors,
        isbn: book.isbn,
        publisher: book.publisher,
        publishedDate: book.publishedDate,
        description: book.description,
        pageCount: book.pageCount,
        categories: book.categories,
        coverImage: book.coverImage,
        language: book.language,
        externalSource: 'google_books',
        externalId: book.externalId,
        status: 'wishlist',
      });
      alert(`"${book.title}" añadido a la colección`);
    } catch (err) {
      alert('Error al añadir: ' + err.message);
    }
  };

  return (
    <div>
      <h1>Buscar Libros</h1>
      <form onSubmit={handleSearch}>
        <input
          type="text"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="Título, autor o ISBN..."
        />
        <button type="submit" disabled={loading}>
          {loading ? 'Buscando...' : 'Buscar'}
        </button>
      </form>

      {error && <p className="error">{error}</p>}

      <div className="results-grid">
        {results.map((book) => (
          <BookCard key={book.externalId} book={book} onAdd={handleAdd} />
        ))}
      </div>
    </div>
  );
}

export default SearchPage;
```

**Step 3: Modificar `frontend/src/App.jsx`**

```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import SearchPage from './pages/SearchPage';

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Mi Colección</Link>
        <Link to="/search">Buscar</Link>
      </nav>
      <Routes>
        <Route path="/" element={<h1>Mi Colección de Libros</h1>} />
        <Route path="/search" element={<SearchPage />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

**Step 4: Commit**

```bash
git add frontend/src/
git commit -m "feat(frontend): add search page with book cards"
```

---

### Task 9: Crear página de Mi Colección

**Objective:** Mostrar los libros guardados en la BD con filtros y acciones.

**Files:**
- Crear: `frontend/src/pages/CollectionPage.jsx`
- Modificar: `frontend/src/App.jsx`

**Step 1: Crear `frontend/src/pages/CollectionPage.jsx`**

```jsx
import { useState, useEffect } from 'react';
import { booksService } from '../services/booksService';
import BookCard from '../components/BookCard';

function CollectionPage() {
  const [books, setBooks] = useState([]);
  const [loading, setLoading] = useState(true);
  const [statusFilter, setStatusFilter] = useState('');

  const fetchBooks = async () => {
    setLoading(true);
    try {
      const params = statusFilter ? { status: statusFilter } : {};
      const data = await booksService.getAll(params);
      setBooks(data.data);
    } catch (err) {
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    fetchBooks();
  }, [statusFilter]);

  const handleDelete = async (id) => {
    if (!window.confirm('¿Eliminar este libro?')) return;
    try {
      await booksService.delete(id);
      fetchBooks();
    } catch (err) {
      alert('Error al eliminar');
    }
  };

  return (
    <div>
      <h1>Mi Colección</h1>
      <div className="filters">
        <select value={statusFilter} onChange={(e) => setStatusFilter(e.target.value)}>
          <option value="">Todos</option>
          <option value="wishlist">Lista de deseos</option>
          <option value="reading">Leyendo</option>
          <option value="completed">Completados</option>
          <option value="abandoned">Abandonados</option>
        </select>
      </div>

      {loading ? (
        <p>Cargando...</p>
      ) : books.length === 0 ? (
        <p>No hay libros. ¡Busca y añade algunos!</p>
      ) : (
        <div className="results-grid">
          {books.map((book) => (
            <div key={book._id}>
              <BookCard book={book} />
              <button onClick={() => handleDelete(book._id)}>Eliminar</button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}

export default CollectionPage;
```

**Step 2: Modificar `frontend/src/App.jsx`**

```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import SearchPage from './pages/SearchPage';
import CollectionPage from './pages/CollectionPage';

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Mi Colección</Link>
        <Link to="/search">Buscar</Link>
      </nav>
      <Routes>
        <Route path="/" element={<CollectionPage />} />
        <Route path="/search" element={<SearchPage />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

**Step 3: Commit**

```bash
git add frontend/src/
git commit -m "feat(frontend): add collection page with filters"
```

---

### Task 10: Tests del backend

**Objective:** Añadir tests de integración para los endpoints del backend.

**Files:**
- Crear: `backend/tests/books.test.js`

**Step 1: Escribir tests**

```javascript
const request = require('supertest');
const mongoose = require('mongoose');
const app = require('../src/app');
const Book = require('../src/models/Book');

beforeAll(async () => {
  await mongoose.connect(process.env.MONGODB_URI_TEST || 'mongodb://localhost:27017/wiki-collection-test');
});

afterAll(async () => {
  await mongoose.connection.close();
});

beforeEach(async () => {
  await Book.deleteMany({});
});

describe('Books API', () => {
  test('GET /api/books - lista vacía', async () => {
    const res = await request(app).get('/api/books');
    expect(res.status).toBe(200);
    expect(res.body.data).toEqual([]);
  });

  test('POST /api/books - crear libro', async () => {
    const book = { title: 'Test Book', authors: ['Author'], status: 'wishlist' };
    const res = await request(app).post('/api/books').send(book);
    expect(res.status).toBe(201);
    expect(res.body.title).toBe('Test Book');
  });

  test('POST /api/books - error sin título', async () => {
    const res = await request(app).post('/api/books').send({ authors: ['Author'] });
    expect(res.status).toBe(400);
  });

  test('PUT /api/books/:id - actualizar libro', async () => {
    const created = await request(app).post('/api/books').send({ title: 'Original', authors: ['A'] });
    const res = await request(app)
      .put(`/api/books/${created.body._id}`)
      .send({ title: 'Actualizado' });
    expect(res.status).toBe(200);
    expect(res.body.title).toBe('Actualizado');
  });

  test('DELETE /api/books/:id - eliminar libro', async () => {
    const created = await request(app).post('/api/books').send({ title: 'To Delete', authors: ['A'] });
    const res = await request(app).delete(`/api/books/${created.body._id}`);
    expect(res.status).toBe(200);
  });
});
```

**Step 2: Ejecutar tests**

```bash
cd backend && npm test
```

Expected: 5 passed

**Step 3: Commit**

```bash
git add backend/tests/
git commit -m "test(backend): add books API integration tests"
```

---

## Riesgos y Decisiones Abiertas

| Riesgo | Mitigación |
|--------|------------|
| Google Books API Key rate limit | Implementar cache en backend + fallback a Open Library |
| MongoDB no disponible localmente | Usar MongoDB Atlas (cloud gratuito) o Docker |
| Datos inconsistentes entre APIs externas | Normalizar en el controller antes de guardar |
| CORS en desarrollo | Proxy de Vite configurado en Task 6 |

## Próximos Pasos (Fase 2)

- Videojuegos: usar RAWG API (gratuita) para búsqueda
- Reutilizar estructura MVC creada para libros
- Añadir modelo Game y sus rutas/controller
- Frontend: nueva sección "Videojuegos" con misma UI
