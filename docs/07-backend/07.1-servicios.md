# Servicios

## BookService

Implementa `BookUseCase`. Gestiona CRUD de libros.

```java
public interface BookUseCase {
    Page<Book> search(BookSearchCriteria criteria, Pageable pageable);
    Book findById(String id);
    Book save(Book book);
    Book update(String id, Book updates);
    void delete(String id);
}
```

**Responsabilidades:**
- Búsqueda con criterios (nombre, autor, tipo, estado)
- CRUD completo de libros
- Delegar persistencia al repositorio

## BookSearchService

Implementa `BookSearchUseCase`. Busca libros en Google Books API.

```java
public interface BookSearchUseCase {
    List<BookSearchResult> search(String query);
}
```

**Responsabilidades:**
- Validar query no vacío
- Delegar a `ExternalBookCatalogClient`
- Manejo de errores: propaga excepciones del cliente externo

## GameService

Implementa `GameUseCase`. Gestiona CRUD de juegos.

```java
public interface GameUseCase {
    Page<Game> search(GameSearchCriteria criteria, Pageable pageable);
    Game findById(String id);
    Game save(Game game, boolean obtainPlatinum);
    Game update(String id, Game updates, boolean obtainPlatinum);
    void delete(String id);
}
```

**Nota:** Incluye parámetro `obtainPlatinum` para obtener logros de Steam al crear/actualizar.

## GameSearchService

Implementa `GameSearchUseCase`. Busca juegos con fallback RAWG → FreeToGame.

```java
public interface GameSearchUseCase {
    List<GameSearchResult> search(String query);
}
```

**Implementación:**
1. Intenta primero RAWG (búsqueda por nombre nativa)
2. Si RAWG devuelve lista vacía, usa FreeToGame como fallback
3. No cachea resultados (cada búsqueda hace llamadas frescas)

```java
public List<GameSearchResult> search(String query) {
    if (query == null || query.isBlank()) {
        throw new IllegalArgumentException("El parámetro de búsqueda 'name' es obligatorio");
    }
    List<GameSearchResult> results = rawgClient.search(query);
    if (results.isEmpty()) {
        return freeToGameClient.search(query);
    }
    return results;
}
```

## GameAchievementsService

Implementa `GameAchievementsUseCase`. Obtiene logros de Steam.

```java
public interface GameAchievementsUseCase {
    AchievementsSummary getAchievements(String gameId, String steamId);
}
```

**Responsabilidades:**
- Validar que el juego tenga `steamAppId`
- Obtener esquema de logros del juego (GetSchemaForGame)
- Obtener progreso del jugador (GetPlayerAchievements)
- Combinar ambos en `AchievementsSummary`

## BoardGameService

Implementa `BoardGameUseCase`. Gestiona CRUD de juegos de mesa.

```java
public interface BoardGameUseCase {
    Page<BoardGame> search(BoardGameSearchCriteria criteria, Pageable pageable);
    BoardGame findById(String id);
    BoardGame save(BoardGame boardGame);
    BoardGame update(String id, BoardGame updates);
    void delete(String id);
}
```

## BoardGameSearchService

Implementa `BoardGameSearchUseCase`. Busca juegos de mesa con fallback BGG JSON → BGG XML.

```java
public interface BoardGameSearchUseCase {
    List<BoardGameSearchResult> search(String query);
}
```

**Implementación:**
1. Intenta primero BGG JSON (búsqueda por nombre nativa)
2. Si BGG JSON devuelve lista vacía, usa BGG XML como fallback
3. No cachea resultados

## MagicCardService

Implementa `MagicCardUseCase`. Gestiona CRUD de cartas Magic.

```java
public interface MagicCardUseCase {
    Page<MagicCard> search(MagicCardSearchCriteria criteria, Pageable pageable);
    MagicCard findById(String id);
    MagicCard save(MagicCard card);
    MagicCard update(String id, MagicCard updates);
    void delete(String id);
}
```

## MagicCardSearchService

Implementa `MagicCardSearchUseCase`. Busca cartas en Scryfall.

```java
public interface MagicCardSearchUseCase {
    List<MagicCardSearchResult> search(String query);
}
```

**Responsabilidades:**
- Validar query no vacío
- Delegar a `ExternalMagicCardCatalogClient`
- Manejo de errores: devuelve lista vacía si falla

## GoogleBooksClient

Implementa `ExternalBookCatalogClient`. Cliente para Google Books API.

```java
public interface ExternalBookCatalogClient {
    List<BookSearchResult> search(String query);
}
```

**Características:**
- Usa `RestClient` para llamadas HTTP
- Búsqueda con `intitle:` + `langRestrict=es` + `maxResults=10`
- Reintentos: 4 intentos totales con intervalo de 1 segundo
- Propaga excepciones si todos los intentos fallan

## RAWGClient

Implementa `ExternalGameCatalogClient`. Cliente para RAWG API.

```java
public interface ExternalGameCatalogClient {
    List<GameSearchResult> search(String query);
}
```

**Características:**
- Usa `RestClient` para llamadas HTTP
- Mapeo de plataformas limitado (PC, PS2, PS3, WII_U, SWITCH)
- Manejo de errores: devuelve lista vacía si falla

## FreeToGameClient

Implementa `ExternalGameCatalogClient`. Cliente para FreeToGame API.

**Características:**
- Usa `RestClient` para llamadas HTTP
- Mapeo de plataforma: solo detecta PC
- Manejo de errores: devuelve lista vacía si falla

## BggJsonClient

Implementa `ExternalBoardGameCatalogClient`. Cliente para BGG JSON API.

**Características:**
- Usa `RestTemplate` para llamadas HTTP
- JSON nativo (más sencillo)
- Manejo de errores: devuelve lista vacía si falla

## BggXmlClient

Implementa `ExternalBoardGameCatalogClient`. Cliente para BGG XML API.

**Características:**
- Usa `RestTemplate` con cookies de autenticación
- Usa Jackson XML para parsear respuestas
- Implementa reintentos en caso de 202 Accepted
- Manejo de errores: devuelve lista vacía si falla

## ScryfallClient

Implementa `ExternalMagicCardCatalogClient`. Cliente para Scryfall API.

```java
public interface ExternalMagicCardCatalogClient {
    List<MagicCardSearchResult> search(String query);
    MagicCard findById(String id);
    MagicCard findByName(String name);
}
```

**Características:**
- Usa `RestTemplate` para llamadas HTTP
- Búsqueda fuzzy por nombre
- Búsqueda avanzada con query
- Obtener por ID
- Reintentos automáticos (3 intentos, delay 1s)

## SteamAchievementsClient

Implementa `SteamCatalogueClient`. Cliente para Steam API.

```java
public interface SteamCatalogueClient {
    Long searchGameByName(String name);
    List<SteamAchievement> getPlayerAchievements(Long appId, String steamId);
    List<SteamAchievement> getGameSchema(Long appId);
}
```

**Características:**
- Usa `RestClient` para llamadas HTTP
- Dos clientes: `steamRestClient` (Web API) y `steamStoreRestClient` (Store search)
- Manejo de errores: devuelve null/lista vacía si falla
